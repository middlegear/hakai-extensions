import { USER_AGENT_HEADER, ACCEPT_HEADER, ACCEPT_ENCODING } from "./headers";

import {
  anitaku_ACCEPT_ENCODING_HEADER,
  anitaku_ACCEPT_HEADER,
  anitaku_USER_AGENT_HEADER,
} from "./headers";

import { zoroClient } from "./zoroclient";
import { anitakuClient } from "./anitakuClient";

export {
  anitaku_ACCEPT_ENCODING_HEADER,
  anitaku_ACCEPT_HEADER,
  anitaku_USER_AGENT_HEADER,
  USER_AGENT_HEADER,
  ACCEPT_HEADER,
  ACCEPT_ENCODING,
  zoroClient,
  anitakuClient,
};
